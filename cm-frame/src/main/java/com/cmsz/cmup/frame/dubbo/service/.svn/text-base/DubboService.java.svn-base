package com.cmsz.cmup.frame.dubbo.service;

import java.util.Map;

import com.cmsz.cmup.frame.model.ReturnResult;

/**
 * dubbo基础接口类
 * 所有子系统对外提供的dubbo接口需要继承此接口，不需要添加任何方法
 *
 * @author JinChao
 * 
 * @date 2015年11月11日 上午9:58:45
 *
 */
public interface DubboService {
	
	/**
	 * 执行dubbo服务
	 * @param variableMap 参数map
	 * @return ReturnResult 执行结果
	 */
	public ReturnResult doDubboService(Map<String, String> variableMap);
}
